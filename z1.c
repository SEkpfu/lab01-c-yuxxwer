#include ‹stdio.h›
#include ‹math.h>
int main(){
double a,b,c;
printf("a= ");
scanf("%1f", &a);
double volume=a*a*a;
double area=6*a*a;
printf("volume=%f, area=%f" , volume, area);
return 0;
}
#include <stdio.h>
#include <math.h>
int main(){
    int a,b,c;
    printf("Введите три числа =");
    scanf("%d %d %d",&a,&b,&c);
    int sum, pr;  
    a%2 !=0 ? printf("%d %d", sum = b + c, pr =b*c) : printf("%d", (a >c ? c : a));
    return 0;
}